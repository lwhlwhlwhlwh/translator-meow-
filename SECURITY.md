# 安全策略

## 支持范围

项目当前处于早期版本阶段，仅维护默认分支上的最新代码。历史构建和第三方分发包不提供安全更新保证。

## 报告漏洞

请优先使用 GitHub 仓库的 Private vulnerability reporting。仓库尚未启用该功能时，请联系维护者并只提供最小复现信息；不要在公开 Issue 中发布 API 密钥、个人文档、内部接口地址或完整利用代码。

报告应包括受影响版本、运行环境、复现步骤、实际影响和建议缓解方式。维护者确认安全问题后，会在修复可用时协调披露。

## 使用边界

- 翻译内容会发送到用户配置的第三方接口，服务商的数据处理政策不由本项目控制。
- API 密钥使用 Electron `safeStorage` 保存，但本机账户或操作系统已失陷时不能提供额外隔离。
- 自定义接口地址是受信任的用户配置。组织环境应结合代理、防火墙和终端策略限制可访问网络。
- 未签名开发构建不应被视为经过发布者身份验证的正式安装包。
